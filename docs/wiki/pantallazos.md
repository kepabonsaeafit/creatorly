# Pantallazos

Pantallazos de las **3 secciones más importantes** de la aplicación. Esta página se completa cuando las páginas estén construidas (Fase 2 del plan).

> 📌 **Cómo tomar las capturas:** con `npm run dev` corriendo y los datos semilla cargados, capturar a pantalla completa del navegador (F11) para que se vea la navegación y el contenido.

## 1. Pedidos (listado con selector + tabla + gráfico)

<!-- Subir aquí la captura de /pedidos con filtros aplicados, la tabla y el gráfico de barras -->

- [ ] Captura de `/pedidos` con un filtro aplicado (por estado o marca)
- [ ] Deben verse: selector, tabla y gráfico Chart.js en la misma pantalla

## 2. Reportes (selector + tablas + 4 gráficos)

<!-- Subir aquí la captura de /reportes con los 4 gráficos -->

- [ ] Captura de `/reportes` con los gráficos de: pedidos por estado (pie), por creador (barras), presupuesto por marca (barras horizontales) y por mes (línea)

## 3. Login (acceso con roles)

<!-- Subir aquí la captura de /login -->

- [ ] Captura de `/login`
- [ ] Opcional: segunda captura mostrando la vista de administrador con las páginas solo-admin visibles en la navegación
